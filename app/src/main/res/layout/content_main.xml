<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.nenek.oamapplication.MainActivity"
    tools:showIn="@layout/activity_main">

    <ImageView
        android:id="@+id/imgfarlycakep"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:src="@drawable/main_image"
        android:scaleType="centerCrop"/>

    <Button
        android:id="@+id/btnkakekcakep"
        android:layout_below="@+id/imgfarlycakep"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:layout_width="350dp"
        android:layout_height="60dp"
        android:text="Pesan Ambulans"
        android:textColor="#ffffff"
        android:textSize="40px"
        android:background="@drawable/buttonpesan"/>

    <Button
        android:id="@+id/btnkakekcakepdua"
        android:layout_below="@+id/btnkakekcakep"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:layout_width="350dp"
        android:layout_height="60dp"
        android:text="Akun Saya"
        android:textColor="#ffffff"
        android:textSize="40px"
        android:background="@drawable/buttonpesan"/>

    <Button
        android:id="@+id/btnkakekcakeptiga"
        android:layout_below="@+id/btnkakekcakepdua"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:layout_width="350dp"
        android:layout_height="60dp"
        android:text="Informasi Aplikasi"
        android:textColor="#ffffff"
        android:textSize="40px"
        android:background="@drawable/buttonpesan"/>
    
</RelativeLayout>
